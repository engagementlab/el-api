html
  head
    title= 'Engagement Lab CMS'

  body(style='background:#FAFBFB' onload='onload()')
    div
      span Select an app
    each val,index in apps
      div 
        a(href='cms/' + val.dir) #{val.name}