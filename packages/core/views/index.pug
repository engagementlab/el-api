include ./header.pug

+header()

.ui.segment
  if notAdmin
    h3 You do not have permissions for /admin.
  else

    if noPermission
      h3 You have no permissions for that app, select another.

    div
      span Select an app:
    
    if noAccess
      h3 You have no access to any app!
    else
      each val,index in apps
        div 
          a(href='/cms/@/' + index) #{val.name}